#include<iostream>
#include<string>
using namespace std;
int main () {
    int n;
    string s[207];
    while (cin >> n) {
        if (n == 0) {
            return 0;
        }
        for (int i = 0;i<n;i++) {
            cin >> s[i];
            
        }
    }
}